import { expect } from 'chai'
import * as gameOfLife from './gameOfLife'

describe('setup test', () => {
  it('when everything is setup correctly I will be green', () => {
    expect('everything').to.be.ok;
  })
})